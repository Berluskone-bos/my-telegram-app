# План: ТЗ 4.7, 4.8, 4.9, 4.10

## Текущее состояние (из исследования)

### 4.7 Оформление заказа
**Есть:** `index.html:203-229` — 3 поля: регион (select), адрес (одно поле), телефон
**Логика:** `js/app.js:480-537` — `makeOrder()`, отправка через `Telegram.WebApp.sendData()`
**Нет:** Имя, выбор доставка/самовывоз, разделённые поля адреса, комментарий, способ оплаты

### 4.8 Статусы заказа
**Есть:** `js/profile.js:188` — hardcoded "Новый" для всех заказов
**Нет:** Системы статусов вообще, уведомлений, отслеживания

### 4.9 Мои заказы
**Есть:** `js/profile.js:149-201` — список заказов, редактирование/удаление
**Нет:** Кнопки «ЗАКАЗАТЬ ПОВТОРНО», предупреждений об изменении цены

### 4.10 Избранное
**Есть:** `js/favorites.js` — полная реализация (toggle, render, localStorage)
**Нет:** Только свайп-удаление (опционально)

---

## Реализация

### Шаг 1: Форма заказа (4.7)
**Файл: `index.html:203-229`**

Заменить текущую форму:
```html
- Имя (input, автозаполнение из Telegram)
- Телефон (input, +7)
- Доставка / Самовывоз (radio)
- Адрес (при доставке):
  - Город (input)
  - Улица (input)  
  - Дом (input)
  - Подъезд (input)
  - Квартира (input)
  - Комментарий (textarea)
- Способ оплаты (select): Наличные, Карта, SBP
- Кнопка «ПОДТВЕРДИТЬ ЗАКАЗ»
```

### Шаг 2: Логика заказа (4.7)
**Файл: `js/app.js:480-537`**

- Обновить `makeOrder()` для сбора новых полей
- Добавить валидацию (имя, телефон обязательны)
- При самовывозе — адрес не обязателен
- Обновить объект заказа с новыми полями

### Шаг 3: Система статусов (4.8)
**Файл: `js/profile.js`**

- Добавить константы:
  - OrderStatus: NEW, CONFIRMED, ASSEMBLING, SHIPPED, DELIVERED, CANCELLED
  - PaymentStatus: PENDING, PAID, FAILED, REFUNDED
- Добавить поля `status` и `paymentStatus` в объект заказа (по умолчанию NEW/PENDING)
- Добавить `getStatusText(status)` — текст статуса на русском
- Добавить `getStatusClass(status)` — CSS класс для бейджа
- Обновить `renderOrderHistory()` — показывать реальный статус
- Добавить статусы оплаты в карточку заказа

### Шаг 4: Кнопка «ЗАКАЗАТЬ ПОВТОРНО» (4.9)
**Файл: `js/profile.js:178-197`**

- Добавить кнопку в карточку заказа
- Функция `reorder(idx)`:
  - Проверить наличие товаров в каталоге
  - Проверить изменение цен
  - Добавить товары в корзину
  - Показать предупреждения если цена изменилась
  - Показать «ПОКАЗАТЬ ПОХОЖИЕ» если товар отсутствует

### Шаг 5: CSS
**Файл: `css/styles.css`**

- Стили для radio buttons (Доставка/Самовывоз)
- Стили для полей адреса (сетка 2 колонки)
- Стили для статусов (цветовые бейджи):
  - NEW — серый
  - CONFIRMED — синий
  - ASSEMBLING — жёлтый
  - SHIPPED — оранжевый
  - DELIVERED — зелёный
  - CANCELLED — красный
- Стили для кнопки «ЗАКАЗАТЬ ПОВТОРНО»
- Стили для textarea (комментарий)

---

## Файлы для изменения

1. `index.html:203-229` — форма заказа
2. `js/app.js:480-537` — `makeOrder()`
3. `js/profile.js` — статусы, reorder, рендер
4. `css/styles.css` — новые стили

---

## Проверка

1. Корзина → форма с разделёнными полями адреса
2. Выбрать «Самовывоз» → адрес скрывается
3. Оформить заказ → все поля сохраняются
4. «Мои заказы» → статус отображается
5. «ЗАКАЗАТЬ ПОВТОРНО» → товары в корзине
6. Мобильный экран (320px) → всё работает
